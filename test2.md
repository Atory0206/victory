```mermaid
classDiagram 
    direction BT %% TB, BT, RL, LR 모두 가능
   class Person{
        +name: str
        +phoneNumber: str
        +emailAddress: str
        +purchaseParkingPass()
    }
```